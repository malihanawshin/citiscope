package com.example.imm.mypractice;

import android.content.Context;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.support.v7.widget.GridLayoutManager;
import android.support.v7.widget.LinearLayoutManager;
import android.support.v7.widget.RecyclerView;
import android.support.v7.widget.Toolbar;
import android.view.Menu;
import android.view.MenuItem;

import java.util.ArrayList;

import static android.support.v7.widget.GridLayoutManager.*;

public class MainActivity extends AppCompatActivity {

    // this class is of no use now

/*
    private RecyclerView serviceList;
    private Context mContext;
    //ServiceListAdapter adapter;
    //ArrayList<Service> items = new ArrayList<Service>();

    //Service tuitor = new Service("Tuition","");
    //Service blooddonation = new Service("Blood Donation","");
    //Service apartment = new Service("Apartment Renting","");
    //Service  doctor = new Service("Doctor","");

    //Toolbar myToolbar = (Toolbar) findViewById(R.id.my_toolbar);
    //setSupportActionBar(myToolbar);

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        //serviceList = (RecyclerView) findViewById(R.id.services);

        //GridLayoutManager manager = new GridLayoutManager(mContext,3 );
        //serviceList.setLayoutManager(manager);

        //adapter = new ServiceListAdapter(mContext, items);
        //serviceList.setAdapter(adapter);
    }

    @Override
    public boolean onCreateOptionsMenu(Menu menu) {
        getMenuInflater().inflate(R.menu.overflow_menu, menu);
        return true;
    }

    @Override
    public boolean onOptionsItemSelected(MenuItem item) {
        switch (item.getItemId()) {
            case R.id.action_sign_in:
                // sign in / login page selected
                return true;
            case R.id.action_sign_out:
                // sign out action  was selected
                return true;
            case R.id.action_sign_up:
                // register page selected
                return true;
            case R.id.action_password_change:
                // password change option selected
                return true;
            default:
                return super.onOptionsItemSelected(item);
        }
    }
*/

}
